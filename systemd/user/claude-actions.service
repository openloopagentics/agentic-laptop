[Unit]
Description=Answer agent prompts from the phone (tailnet only)
After=network-online.target

[Service]
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=%h/.local/bin/claude-actiond
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
