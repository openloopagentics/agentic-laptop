[Unit]
Description=Refresh the Touch Bar commit counts every 10 minutes

[Timer]
OnStartupSec=45s
OnUnitActiveSec=10min
AccuracySec=30s
# Catch up after resume rather than waiting a full interval.
Persistent=true

[Install]
WantedBy=timers.target
